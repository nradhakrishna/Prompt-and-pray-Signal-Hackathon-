[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "distributed-control-worker-runtime"
version = "0.1.0"
description = "Integrated deterministic control plane and recoverable worker runtime"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["control*", "shared*", "worker*", "oracle*"]
